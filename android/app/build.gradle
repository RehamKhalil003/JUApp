// android/app/build.gradle (App-level)

plugins {
    id 'com.android.application'
    id 'kotlin-android'
    id 'dev.flutter.flutter-gradle-plugin'
    id 'com.google.gms.google-services'  // Apply the Google services plugin
}

android {
    namespace = "com.blueray.StudentRegistrationApp"
    compileSdk = 35
    ndkVersion = "27.0.11902837"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.blueray.StudentRegistrationApp"
        minSdk = flutter.minSdkVersion
        targetSdk = 35
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
    signingConfigs {
        release {
            storeFile file('/Users/rehamkhalil/AndroidStudioProjects/my_first_project/android/keystore.jks')
            storePassword '123456'
            keyAlias 'upload'
            keyPassword '123456'
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
    }
}

dependencies {
    // Firebase BoM and other dependencies
    implementation platform("com.google.firebase:firebase-bom:33.12.0")
    implementation "com.google.firebase:firebase-analytics"
}

// Apply the Google services plugin here
apply plugin: 'com.google.gms.google-services'
